CREATE PROCEDURE PROC3 @STORE_CODE INT
AS
SELECT E.EMP_CODE, EMP_TITLE, EMP_FNAME, EMP_LNAME, EMP_INITIAL, EMP_DOB
FROM EMPLOYEE E, STORE S
WHERE E.EMP_CODE = S.EMP_CODE AND S.STORE_CODE = @STORE_CODE

