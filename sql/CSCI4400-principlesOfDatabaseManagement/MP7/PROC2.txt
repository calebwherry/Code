CREATE PROCEDURE PROC2 @STORE_CODE INT
AS
SELECT EMP_TITLE AS TITLE, COUNT(*) AS [NUMBR OF EMPLOYEES]
FROM EMPLOYEE E, STORE S
WHERE E.STORE_CODE = S.STORE_CODE AND S.STORE_CODE = @STORE_CODE
GROUP BY EMP_TITLE
